{
  "name": "talentrank-ats-tracker",
  "version": "0.4.0",
  "private": true,
  "description": "Launch-grade ATS matching prototype with explainable hybrid screening, audit events, and recruiter workflow.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
